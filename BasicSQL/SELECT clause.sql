-- SELECT last_name,    -- like this we can use it explicitly
-- first_name,
-- address,
-- points,
-- (points+10)*10 AS 'Discount factor'
-- FROM customers

SELECT DISTINCT state   -- DISTINCT  keywoard is used to remove the dublicate data
FROM customers